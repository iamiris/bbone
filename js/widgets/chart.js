define([
    'base/app',
    'base'
    ],
    function(app, Base) {
        'use strict';

        //TODO: Line Bar Stacked Bar Staked Area Bar with negative stack Dual Axis with line

        return {
            View: Base.View,
            Model: Base.Model
        };

    });
